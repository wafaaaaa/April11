@model IEnumerable<CYJ.Models.FISCALYEAR>
@{
    ViewBag.Title = "FY";
    Layout = "~/Views/Shared/_mytemplate.cshtml";
}
<h2>Fiscal Years</h2>
<p>
    <div class="col-md-3">
        <input type="button" class="btn btn-info" value="ADD A NEW FISCAL YEAR" onclick="@("window.location.href='" + @Url.Action("Create", "fiscalyears") + "'");" />
    </div>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fy)
        </th>

        <th></th>
    </tr>
    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fy)
        </td>
        <td>
            <button class="btn btn-primary btn-fab btn-fab-mini btn-round" rel="tooltip" title="Click to Edit Team" type="button" style="background-color:#FBAE3B" title="Edit"
                    onclick="location.href='@Url.Action("Edit", new { id = item.fiscalYearID })'">
                <i class="material-icons ">create</i>
            </button>

            <button class="btn btn-primary btn-fab btn-fab-mini btn-round" rel="tooltip" title="Click to Deactivate Team" type="button" style="background-color:#F37158" title="Delete"
                    onclick="location.href='@Url.Action("Delete", new { id = item.fiscalYearID })'">
                <i class="material-icons">delete</i>
            </button>
        </td>
    </tr>
    }
</table>
<div class="col-md-3">
    <input type="button" class="btn btn-info" rel="tooltip" title="Redirects to Add A New Item" value="Back to Add A New Item" onclick="@("window.location.href='" + @Url.Action("Add", "Home") + "'");" />
</div>